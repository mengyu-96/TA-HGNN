这相当于一份"行动纲领"。

一、项目实现逻辑总结（核心思想）

1. 逻辑内核：

· 输入：Wazuh安全日志数据（Linux-APT-Dataset），包含安全事件、规则触发、文件检查、系统配置审计等多维度信息。
· 核心处理：将Wazuh日志中的实体（文件、进程、用户、IP、域名等）和关系（执行、访问、连接等）构建成时序异质图，然后使用时序异质图神经网络（T-HGNN）模型来学习这个图。
· 输出：
  · 节点级别：每个实体是恶意还是良性的概率。
  · 图级别：恶意实体之间的关联关系，并由此聚类出不同的攻击活动和回溯出完整的攻击路径。

2. 工作流逻辑：

· 数据 -> 图 -> 向量 -> 洞察
· 具体流程：Wazuh日志 -> 时序异质图 -> 节点嵌入向量 -> (分类+聚类+路径查找) -> 攻击链可视化

3. 创新逻辑：

· 异质性：让模型理解"进程执行文件"和"进程连接IP"是两种截然不同的行为，从而进行更精细的语义分析。
· 时序性：让模型理解事件的先后顺序，从而具备因果推理能力，能够区分攻击步骤和还原攻击时间线。

---

二、代码应该做到的实现（功能清单）

你的代码需要实现以下四个核心模块，并提供一个端到端的执行流程。下图清晰地描绘了各模块之间的数据流转与功能协作关系：

```mermaid
flowchart LR
    A[Wazuh日志预处理] -->|时序异质图 HeteroData| B[T-HGNN模型]
    B -->|训练好的模型| C[训练与评估]
    C -->|预测结果| D[攻击溯源分析]
    
    subgraph A [Wazuh日志预处理模块]
        A1[解析combine.csv] --> A2[提取实体关系] --> A3[构建异质图] --> A4[生成时序快照]
        A1_1[alert: 20K] --> A2
        A1_2[rule: 172] --> A2
        A1_3[file: 44] --> A2
        A1_4[command: 6.7K] --> A2
        A1_5[其他实体] --> A2
    end

    subgraph B [T-HGNN模型架构]
        B1[HGNN编码器<br/>GAT+异质图] --> B2[时序编码器<br/>位置编码+注意力] --> B3[节点分类器<br/>14种节点类型]
    end

    subgraph C [训练与评估模块]
        C1[Focal Loss训练] --> C2[节点分类评估] --> C3[攻击分组评估] --> C4[路径追踪评估]
    end

    subgraph D [攻击溯源分析模块]
        D1[异常检测] --> D2[威胁分类] --> D3[攻击路径重建] --> D4[可视化展示]
    end
```

基于Linux-APT-Dataset的实际数据流：

1. **输入数据**：Wazuh安全日志（91,133条记录，123个特征）
2. **实体提取**：14种节点类型，包括alert、rule、file、command等
3. **关系构建**：基于Wazuh规则和MITRE框架的边关系
4. **时序处理**：基于时间戳生成多个时序快照
5. **模型训练**：T-HGNN学习异质图的时序模式
6. **结果输出**：攻击检测、路径溯源、威胁分类

模块一：数据预处理模块

目标：将Wazuh安全日志数据（Linux-APT-Dataset）转换为PyG库可以处理的HeteroData对象。

必须实现的功能：

1. 读取与解析：读取Linux-APT-Dataset\Linux-APT-Dataset-2024\combine.csv文件，解析Wazuh日志中的实体和关系。
   · 实体类型：alert（安全事件）、rule（规则）、file（文件）、command（命令）、user（用户）、process（进程）、host（主机）、agent（代理）、ip（IP地址）、domain（域名）、port（端口）、mitre_tactic（MITRE战术）、mitre_technique（MITRE技术）等
   · 关系类型：triggered_by（触发）、involves（涉及）、executed（执行）、by_user（用户操作）、at_time（时间）、detected_on（检测位置）等
   · 时间戳：_source.@timestamp、_source.timestamp、_source.predecoder.timestamp等

2. 映射与编码：将字符串类型的实体名和关系类型映射为整数索引，构建异质图的节点和边。

3. 构建异质图数据：
   · 节点特征：为每种节点类型构建特征矩阵，包括原始特征和工程特征
   · 边索引：存储异质图中各种边类型的连接关系
   · 边属性：存储边的时间戳、权重等属性
   · 节点标签：基于Wazuh规则级别、MITRE框架等生成恶意/良性标签

4. 时序快照：根据时间戳将数据分割为多个时序快照，支持时序分析。

模块二：模型架构模块

目标：定义时序异质图神经网络（T-HGNN）模型。

必须实现的功能：

1. 异质图编码器（HGNN Encoder）：
   · 实现基于GAT的异质图卷积层，能处理多种节点和边类型。
   · 输入：异质图的节点特征、边索引、边类型。
   · 输出：经过异质图卷积聚合后的节点表征。
   · 支持14种节点类型：alert、rule、file、command、user、process、host、agent、mitre_tactic、mitre_technique、timestamp、ip、domain、port

2. 时序编码器（Temporal Encoder）：
   · 位置编码：为时间戳生成位置编码
   · 时序注意力：使用时间差信息增强注意力机制
   · 时序卷积：捕获时序模式
   · 时序融合：将时序信息与图结构信息融合
   · 输入：边时间戳、节点表征。
   · 输出：融合了时序信息的节点表征。

3. 节点分类器（Node Classifier）：
   · 为每种节点类型设计专门的分类器。
   · 输入：最终得到的节点表征。
   · 输出：每个节点属于恶意类的概率。

模块三：训练与评估模块

目标：训练模型并评估其性能。

必须实现的功能：

1. 训练循环：
   · 前向传播，计算损失（使用Focal Loss处理类别不平衡）。
   · 反向传播，优化模型参数。
   · 早停机制，防止过拟合。
   · 学习率调度，优化训练过程。
   · 记录训练损失和验证集指标。

2. 评估函数：
   · 节点分类评估：准确率、精确率、召回率、F1-Score、AUC-ROC、AP、MCC等指标
   · 攻击分组评估：NMI、ARI等聚类指标
   · 路径追踪评估：成功率、路径相似度、平均源节点数、路径长度等指标
   · 异常检测评估：检测率、误报率等指标

模块四：溯源分析与可视化模块

目标：利用训练好的模型进行深入的攻击分析。

必须实现的功能：

1. 攻击者分组（聚类）：
   · 提取模型输出的节点表征。
   · 使用聚类算法（如DBSCAN、K-Means）对所有恶意节点的表征进行聚类。
   · 计算NMI和ARI，评估聚类效果。
   · 基于Wazuh规则和MITRE框架进行攻击模式识别。

2. 攻击路径溯源：
   · 实现一个回溯算法。从一个给定的恶意节点（攻击终点）开始，在图中沿着时间逆序和模型计算出的关联强度，寻找最有可能连接到攻击源头的路径。
   · 基于时序信息进行因果推理，重建攻击时间线。
   · 计算回溯成功率、路径相似度、平均源节点数、路径长度等溯源专属指标。

3. 异常检测与威胁分类：
   · 基于节点嵌入进行异常检测。
   · 使用MITRE ATT&CK框架进行威胁分类。
   · 实现实时威胁监控和告警。

4. 可视化：
   · 使用NetworkX或Gephi等工具，将关键的溯源路径图绘制出来。
   · 在图中用不同的颜色和形状区分节点类型和恶意性，让攻击链一目了然。
   · 提供交互式攻击故事板，展示攻击的完整时间线。

---

最终交付物

1. 一个端到端可运行的Python项目，包含上述所有模块：
   · 基于Wazuh日志的时序异质图构建
   · T-HGNN模型实现（HGNN编码器 + 时序编码器 + 节点分类器）
   · 完整的训练和评估流程
   · 攻击溯源和可视化功能

2. 一份详细的README.md，说明如何安装环境、运行代码和解释结果。

3. 一份实验报告，包含：
   · 所有评估指标的表格（节点分类、攻击分组、路径追踪、异常检测）
   · 关键的溯源路径可视化图
   · 基于Linux-APT-Dataset的实验结果分析
   · 与基线方法的对比分析

4. 技术文档：
   · 系统架构设计文档
   · 数据处理流程文档
   · 模型性能优化指南
   · GPU使用指南

---

三、数据结构分析（基于Linux-APT-Dataset）

根据实际数据分析，Wazuh日志包含以下关键信息：

1. 数据规模：
   · 总记录数：91,133条
   · 特征维度：123列
   · 时间跨度：2023年10月1日的数据

2. 主要实体类型：
   · alert：安全事件（主要实体，20,000个）
   · rule：安全规则（172个）
   · file：文件实体（44个）
   · command：命令实体（6,699个）
   · user：用户实体（87个）
   · process：进程实体（38个）
   · host：主机实体（7个）
   · agent：代理实体（7个）
   · ip：IP地址（12个）
   · domain：域名（33个）
   · port：端口（4个）
   · mitre_tactic：MITRE战术（10个）
   · mitre_technique：MITRE技术（67个）

3. 关键字段：
   · 时间戳：_source.@timestamp、_source.timestamp、_source.predecoder.timestamp
   · 规则信息：_source.rule.level、_source.rule.description、_source.rule.groups
   · 文件信息：_source.data.file、_source.syscheck.path
   · 命令信息：_source.data.command
   · 用户信息：_source.data.srcuser、_source.data.dstuser
   · 网络信息：_source.data.srcip、_source.data.srcport
   · MITRE框架：_source.rule.mitre.tactic、_source.rule.mitre.technique

4. 标签生成策略：
   · 基于Wazuh规则级别（_source.rule.level）进行初步分类
   · 结合MITRE ATT&CK框架进行威胁分类
   · 使用文件完整性检查（syscheck）结果作为辅助标签
   · 通过数据增强技术平衡正负样本比例

这个总结清晰地指明了基于Wazuh安全日志的时序异质图神经网络项目的实现方向和最终需要交付的成果。