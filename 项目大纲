系统逻辑重构：基于时序异质图神经网络的APT攻击溯源系统

一、核心洞察：为什么传统方法失效，我们如何破局？

1. 根本矛盾：APT攻击的低速率、长周期、强隐蔽特性，与传统安全工具基于瞬时、孤立事件的检测模式之间存在不可调和的矛盾。
2. 关键突破口：攻击者可以伪装单个事件，但无法伪装整个攻击链的因果逻辑。真正的核心在于实体间随时间推移的交互关系。
3. 我们的范式转换：
   · 从：分析孤立的事件警报。
   · 到：构建并分析整个系统的动态交互图谱，将安全分析从“事件驱动”升级为“故事驱动”。

二、逻辑架构：分层解耦，各司其职

整个系统遵循一个清晰的分层架构，如下图所示，它体现了从数据到洞察的完整价值链条：

```mermaid
flowchart TD
    A[原始日志<br>（auditd, Sysmon）] --> B[数据预处理层]

    subgraph B [数据预处理层]
        B1[解析与清洗] --> B2[实体关系抽取] --> B3[时序异质图构建<br>（HeteroData对象）]
    end

    B --> C[核心算法层]

    subgraph C [核心算法层]
        C1[异质图编码器<br>（HGNN）] --结合--> C2[时序编码模块<br>（Temporal Encoder）] --> C3[节点分类器<br>（MLP）]
    end

    C --> D[智能应用层]
    D --> E[攻击检测<br>（恶意节点识别）]
    D --> F[攻击溯源<br>（路径重构）]
    D --> G[攻击聚类<br>（活动归因）]

    E & F & G --> H[可视化呈现层<br>（攻击故事看板）]
```

第一层：数据预处理层（从“混沌”到“秩序”）

· 输入：多源、异构的原始系统日志（如Linux auditd）。
· 核心任务：进行实体关系抽取，将非结构化的日志流转化为结构化的时序异质图。
· 关键输出：一个PyG HeteroData 对象，其中包含：
  · node_dict: 不同类型的节点（进程、文件、网络节点等）及其索引。
  · edge_index_dict: 不同类型的边（执行、读写、连接等）的连接关系。
  · edge_attr_dict: 边的属性，核心是时间戳。
· 逻辑价值：此层是将物理世界的系统行为映射为数字世界可计算图结构的基础，是后续所有智能分析的“基石”。

第二层：核心算法层（从“结构”到“洞察”）

· 输入：预处理层生成的时序异质图。
· 核心任务：通过我们设计的 T-HGNN模型，学习图中每个节点的动态上下文表征向量。
· 模型三要素：
  1. 异质图编码器（HGNN）：理解语义。采用HGT或HGAT等模型，尊重不同类型节点和关系的差异，进行信息传播与聚合。
  2. 时序编码模块：理解因果。通过随机位置编码（RPE） 或时序注意力，将时间戳信息注入消息传递过程，使模型感知事件发生的先后顺序。
  3. 节点分类器：输出概率。最终通过一个多层感知机（MLP）将节点的表征向量映射为恶意/正常的概率。
· 逻辑价值：此层是系统的“大脑”，其核心能力不是记忆规则，而是通过表征学习捕捉正常与恶意行为在拓扑结构和时序模式上的微妙差异。

第三层：智能应用层（从“洞察”到“行动”）

· 输入：核心算法层输出的节点表征向量和分类概率。
· 三大应用任务：
  1. 攻击检测：根据节点的恶意概率，直接识别出受害主机上的恶意实体。
  2. 攻击溯源：这是项目的精髓。从一个高置信度的恶意节点（如数据外泄点）出发，沿时间逆序，在图上游走。基于模型学习到的关联强度，选择最可能的路径进行回溯，直至定位攻击入口点，还原攻击链。
  3. 攻击聚类：将所有检测到的恶意节点的表征向量进行聚类（如DBSCAN），同一簇的节点很可能属于同一次攻击活动，从而实现攻击活动的归因分析。
· 逻辑价值：此层将模型产生的“智能”转化为安全分析师可理解的“结论”和可采取的“行动”。

第四层：可视化呈现层（从“行动”到“认知”）

· 输入：应用层输出的攻击链、恶意节点列表和聚类结果。
· 核心任务：利用图形化工具（如Gephi, Neo4j, ECharts）将分析结果可视化，生成攻击故事看板。
· 呈现内容：清晰的攻击步骤图、关键节点标注、时间线、聚类结果等。
· 逻辑价值：降低认知门槛，为安全分析师提供直观、可信的决策支持，完成从数据到智慧的最后一公里。

三、工作流程：一环扣一环的精密管道

项目的执行过程遵循一个单向依赖、逐步递进的管道，如下图所示：

```mermaid
flowchart LR
    A[阶段一<br>数据工程] --> B[阶段二<br>模型研发] --> C[阶段三<br>系统实现] --> D[阶段四<br>评估验证]

    subgraph A [阶段一：数据工程]
        A1[数据获取] --> A2[图谱构建] --> A3[特征工程]
    end

    subgraph B [阶段二：模型研发]
        B1[模型结构设计] --> B2[训练与验证] --> B3[推理与应用]
    end

    subgraph C [阶段三：系统实现]
        C1[后端服务] --> C2[前端看板] --> C3[系统集成]
    end

    subgraph D [阶段四：评估验证]
        D1[指标量化] --> D2[对比实验] --> D3[案例研究]
    end
```

四、项目独特性与价值总结

· 系统性：并非单一的算法改进，而是一个端到端的系统解决方案，涵盖了从数据到应用的全流程。
· 深度融合：不是简单套用GNN，而是抓住了APT检测中的异质性和时序性两个关键挑战，并进行了针对性的模型设计。
· 可解释性：最终的输出不是简单的0和1，而是一条可解释、可验证的攻击路径，极大地提升了结果的可信度和实用性。
· 前瞻性：理念上从“防御者思维”转向“攻击者思维”，专注于理解攻击者的战术、技术和 procedures（TTPs），代表了威胁检测的未来方向。