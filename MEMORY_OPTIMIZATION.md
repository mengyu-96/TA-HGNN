# T-HGNN 内存优化指南

## 概述

本文档提供了T-HGNN系统的内存优化方案，帮助在内存受限的环境中运行系统。

## 内存优化策略

### 1. 数据层面优化

#### 数据采样
- **原始设置**: 处理50,000行数据
- **优化设置**: 处理20,000行数据
- **内存节省**: 约60%的数据内存占用

#### 批次处理
- **原始设置**: 批次大小1,000
- **优化设置**: 批次大小500
- **内存节省**: 减少50%的批次内存占用

#### 时序快照
- **原始设置**: 5个快照
- **优化设置**: 3个快照
- **内存节省**: 减少40%的快照内存占用

### 2. 模型层面优化

#### 模型参数减少
- **隐藏层维度**: 128 → 64 → 32
- **注意力头数**: 8 → 4 → 2
- **网络层数**: 3 → 2 → 1
- **时序嵌入维度**: 32 → 16 → 8

#### 特征维度优化
- **Alert节点**: 16 → 8
- **Command节点**: 16 → 8
- **其他节点**: 64 → 32

### 3. 训练层面优化

#### 批次大小
- **原始设置**: 64
- **优化设置**: 16 → 8
- **内存节省**: 75%的批次内存占用

#### 训练轮数
- **原始设置**: 200轮
- **优化设置**: 50轮
- **时间节省**: 75%的训练时间

## 使用方法

### 方法1: 使用内存优化配置

```bash
python main.py --memory_optimized --mode train --epochs 10 --batch_size 8
```

### 方法2: 使用内存优化脚本

```bash
python run_memory_optimized.py --epochs 10 --batch_size 8 --sample_size 10000
```

### 方法3: 手动配置优化参数

```bash
python main.py \
    --mode train \
    --epochs 10 \
    --batch_size 8 \
    --hidden_dim 32 \
    --num_heads 2 \
    --num_layers 1 \
    --data_path ./Linux-APT-Dataset/Linux-APT-Dataset-2024/combine.csv
```

## 内存监控

系统集成了内存监控功能，可以实时监控内存使用情况：

### 自动内存清理
- 每5个epoch自动检查内存使用率
- 超过阈值时自动执行垃圾回收
- 清理PyTorch缓存
- 清理训练历史记录

### 内存使用报告
- 实时显示内存使用率
- GPU内存使用情况
- 内存优化建议
- 内存使用趋势分析

## 配置对比

| 配置项 | 原始配置 | 标准优化 | 极限优化 |
|--------|----------|----------|----------|
| 数据采样 | 50,000 | 20,000 | 10,000 |
| 批次大小 | 64 | 16 | 8 |
| 隐藏维度 | 128 | 64 | 32 |
| 注意力头数 | 8 | 4 | 2 |
| 网络层数 | 3 | 2 | 1 |
| 快照数量 | 5 | 3 | 2 |
| 训练轮数 | 200 | 50 | 20 |

## 内存使用估算

### 原始配置
- 数据内存: ~680MB
- 模型参数: ~128MB
- 训练内存: ~2GB
- **总计**: ~2.8GB

### 标准优化
- 数据内存: ~270MB
- 模型参数: ~32MB
- 训练内存: ~500MB
- **总计**: ~800MB

### 极限优化
- 数据内存: ~135MB
- 模型参数: ~8MB
- 训练内存: ~200MB
- **总计**: ~343MB

## 性能权衡

### 内存优化带来的影响

#### 正面影响
- 内存使用大幅减少
- 训练速度提升
- 系统稳定性提高
- 支持更多并发任务

#### 负面影响
- 模型精度可能略有下降
- 特征表达能力减弱
- 时序建模能力降低
- 需要更多调参工作

## 建议配置

### 8GB内存系统
```bash
python run_memory_optimized.py --epochs 20 --batch_size 16 --sample_size 20000
```

### 4GB内存系统
```bash
python run_memory_optimized.py --epochs 10 --batch_size 8 --sample_size 10000
```

### 2GB内存系统
```bash
python run_memory_optimized.py --epochs 5 --batch_size 4 --sample_size 5000
```

## 故障排除

### 内存不足错误
1. 进一步减少批次大小
2. 减少数据采样量
3. 使用CPU训练
4. 关闭可视化功能

### 训练速度过慢
1. 适当增加批次大小
2. 减少数据预处理
3. 使用GPU加速
4. 减少模型复杂度

### 模型精度下降
1. 增加训练轮数
2. 调整学习率
3. 使用数据增强
4. 集成多个模型

## 监控命令

### 实时内存监控
```bash
# 查看系统内存使用
htop

# 查看GPU内存使用
nvidia-smi

# 查看Python进程内存
ps aux | grep python
```

### 内存优化验证
```bash
# 运行内存优化测试
python -c "
from src.utils.memory_monitor import MemoryMonitor
monitor = MemoryMonitor()
print('内存状态:', monitor.get_memory_info())
print('优化建议:', monitor.get_memory_recommendations())
"
```

## 总结

通过合理的内存优化配置，T-HGNN系统可以在各种内存受限的环境中稳定运行。建议根据实际硬件配置选择合适的优化级别，并在性能和资源消耗之间找到平衡点。
